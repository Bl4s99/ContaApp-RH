__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
empleados.db
backups/
logs/
empleados_ficticios.csv

# Contiene la contraseña de PostgreSQL en texto plano si se ha configurado
# un servidor propio desde "Base de datos..." -- ver app/db_engine.py.
db_config.json

# Binario compilado (PyInstaller) -- se reconstruye desde el .spec versionado,
# no tiene sentido versionar un binario de ~20 MB.
*.exe
build/
dist/

# Generado por ContaApp RH.spec en cada build a partir de app/version.py --
# no tiene sentido mantener dos copias del número de versión.
version_info.txt

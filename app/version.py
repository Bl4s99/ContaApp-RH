"""Fuente única de verdad del número de versión -- usada tanto por
app/update_check.py (comparar contra la versión publicada en
contaapp.es/version-rh.json) como por ContaApp RH.spec (metadatos de
versión del .exe compilado, ver FileVersion/ProductVersion)."""

VERSION = "1.0.0"
